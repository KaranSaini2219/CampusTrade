# Leave unset for local development; Vite will proxy API and Socket.IO requests.
# For a deployed frontend, use the public HTTPS URL of the Express backend.
VITE_API_URL=https://your-render-service.onrender.com
